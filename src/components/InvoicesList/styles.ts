import styled from 'styled-components';

const StyledInvoicesList = styled.ul`
  margin-top: 48px;
  h2 {
    margin-bottom: 18px;
    font-size: 1.75rem;
    font-weight: bold;
    color: var(--color-text-secondary);
    text-transform: capitalize;
  }
  li {
    margin-bottom: 16px;
  }
`;

export default StyledInvoicesList;
