import styled from 'styled-components';

const StyledDownloadInvoice = styled.button`
  margin-top: -10px;
  display: block;
  width: 100%;
  padding: 16px 0;
  background-color: var(--color-brand);
  border: none;
  outline: none;
  border-radius: 168px;
  color: white;
  cursor: pointer;
  font-weight: bold;
  font-weight: 1.15rem;
`;

export default StyledDownloadInvoice;
